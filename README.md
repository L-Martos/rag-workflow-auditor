# RAG Workflow Auditor (Beginner Edition)

A tiny AI helper that reviews a workflow step and gives quick feedback.
This is a learning project. The goal is to show I can build and run a small AI tool like an engineer—clearly and repeatably.

## Quick Start

### 1) Set up Python
- Python 3.10+ recommended
- Create a virtual environment (optional but recommended)

```bash
# Windows (PowerShell)
python -m venv .venv
.venv\Scripts\Activate.ps1

# Mac/Linux
python3 -m venv .venv
source .venv/bin/activate
